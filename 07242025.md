# 2025-07-24 學習日誌

- [X] 繼續研究 Practicalli Nvim
- [X] 設定 ccc.nvim 色水顯示
- [ ] 讀 Nvim usr_10 manual
- [X] 讀 Lazy.nvim 文件

## Practicalli Nvim

伊个 astrocommunity 無啥好設定，我試過 night-owl theme, neovide, ccc-nvim 等等，
攏無法度直接佇 lua/community.lua 內底个 import 下底行閣添內容做修飾，著愛另外佇
lua/plugins/ 遐另外閣創一个設定檔，而且也真僫設定，需要 Warp AI 鬥相共。

## ccc.nvim 色水顯示

即套佮 `nvim-colorizer.lua` 類似功能，但是嘛會使顯示 ANSI escape color codes。

## Nvim usr_10 manual

原來 command recording 所用个 registers 佮 yank/delete
所用个是仝一套，所以會使直接 put register 來當做文字做修改，
改好閣 yank 轉去原來个 register，就會使 @ 取用改過个指令。

### command recording test

#include <stdio.h>
#include <stdlib.h>
#include <math.h>
network.h
my.h

-  recording as register a
   qa
   I#include <��E>j
   q

-  `"ap` then modify it, and yank to register b
   I#include <A>j

-  try to run @b
   It works!

NOTE: @a 是用 i_<C-e> 跳到行尾，@b 改用 <Esc>A 來做

